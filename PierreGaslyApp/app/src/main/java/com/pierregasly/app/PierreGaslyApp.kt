package com.pierregasly.app

import android.app.Application

class PierreGaslyApp : Application() {
    override fun onCreate() {
        super.onCreate()
        // App-level init (kept minimal for Phase 1)
    }
}
